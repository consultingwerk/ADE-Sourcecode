/** This utility will ensure that the smartobject the attribute is linked to is the same smartobject
    that the object instance is linked to
   
   ------------------------------------------------------------------------------------------------------ **/
/* Simply include the remove_orphans fix program, which does everything that 
   needs to be done for this migration. 
 */
{db/icf/dfd/remove_orphans.p}   


/* E-O-F */