/*********************************************************************
* Copyright (C) 2005 by Progress Software Corporation. All rights    *
* reserved.  Prior versions of this work may contain portions        *
* contributed by participants of Possenet.                           *
*                                                                    *
*********************************************************************/

TRIGGER PROCEDURE FOR DELETE OF gsm_entity_field .

/* Created automatically using ERwin ICF Trigger template db/af/erw/afercustrg.i
   Do not change manually. Customisations to triggers should be placed in separate
   include files pulled into the trigger. ICF auto generates write trigger custom
   include files. Delete or create customisation include files need to be created
   manually. Be sure to put the hook in ERwin directly so as not to have you changes
   overwritten.
   User defined Macro (UDP) Summary (case sensitive)
   gsm_entity_field           Expands to full table name, e.g. gsm_user
   %TableFLA            Expands to table unique code, e.g. gsmus
   %TableObj            If blank or not defined expands to table_obj with no prefix (framework standard)
                        If defined, uses this field as the object field
                        If set to "none" then indicates table does not have an object field
   XYZ                  Do not define so we can compare against an empty string

   See docs for explanation of replication macros 
*/   

&SCOPED-DEFINE TRIGGER_TABLE gsm_entity_field
&SCOPED-DEFINE TRIGGER_FLA gsmef
&SCOPED-DEFINE TRIGGER_OBJ entity_field_obj


DEFINE BUFFER lb_table FOR gsm_entity_field.      /* Used for recursive relationships */
DEFINE BUFFER lb1_table FOR gsm_entity_field.     /* Used for lock upgrades */

DEFINE BUFFER o_gsm_entity_field FOR gsm_entity_field.

/* Standard top of DELETE trigger code */
{af/sup/aftrigtopd.i}

  




/* Generated by ICF ERwin Template */
/* gsm_entity_field has gsm_entity_field_value ON PARENT DELETE CASCADE */
&IF DEFINED(lbe_entity_field_value) = 0 &THEN
  DEFINE BUFFER lbe_entity_field_value FOR gsm_entity_field_value.
  &GLOBAL-DEFINE lbe_entity_field_value yes
&ENDIF
FOR EACH gsm_entity_field_value NO-LOCK
   WHERE gsm_entity_field_value.entity_field_obj = gsm_entity_field.entity_field_obj
   ON STOP UNDO, RETURN ERROR "AF^104^gsmeftrigd.p^delete gsm_entity_field_value":U:
    FIND FIRST lbe_entity_field_value EXCLUSIVE-LOCK
         WHERE ROWID(lbe_entity_field_value) = ROWID(gsm_entity_field_value)
         NO-ERROR.
    IF AVAILABLE lbe_entity_field_value THEN
      DO:
        {af/sup/afvalidtrg.i &action = "DELETE" &table = "lbe_entity_field_value"}
      END.
END.














/* Update Audit Log */
IF CAN-FIND(FIRST gsc_entity_mnemonic
            WHERE gsc_entity_mnemonic.entity_mnemonic = 'gsmef':U
              AND gsc_entity_mnemonic.auditing_enabled = YES) THEN
  RUN af/app/afauditlgp.p (INPUT "DELETE":U, INPUT "gsmef":U, INPUT BUFFER gsm_entity_field:HANDLE, INPUT BUFFER o_gsm_entity_field:HANDLE).

/* Standard bottom of DELETE trigger code */
{af/sup/aftrigendd.i}


/* Place any specific DELETE trigger customisations here */
