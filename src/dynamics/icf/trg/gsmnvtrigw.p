/*********************************************************************
* Copyright (C) 2000 by Progress Software Corporation. All rights    *
* reserved. Prior versions of this work may contain portions         *
* contributed by participants of Possenet.                           *
*               PSC                                                  *
*                                                                    *
*********************************************************************/

TRIGGER PROCEDURE FOR WRITE OF gsm_profile_numeric_value OLD BUFFER o_gsm_profile_numeric_value.

/* Created automatically using ERwin ICF Trigger template db/af/erw/afercustrg.i
   Do not change manually. Customisations to triggers should be placed in separate
   include files pulled into the trigger. ICF auto generates write trigger custom
   include files. Delete or create customisation include files need to be created
   manually. Be sure to put the hook in ERwin directly so as not to have you changes
   overwritten.
   User defined Macro (UDP) Summary (case sensitive)
   gsm_profile_numeric_value           Expands to full table name, e.g. gsm_user
   %TableFLA            Expands to table unique code, e.g. gsmus
   %TableObj            If blank or not defined expands to table_obj with no prefix (framework standard)
                        If defined, uses this field as the object field
                        If set to "none" then indicates table does not have an object field
   XYZ                  Do not define so we can compare against an empty string

   See docs for explanation of replication macros 
*/   

&SCOPED-DEFINE TRIGGER_TABLE gsm_profile_numeric_value
&SCOPED-DEFINE TRIGGER_FLA gsmnv
&SCOPED-DEFINE TRIGGER_OBJ profile_numeric_value_obj


DEFINE BUFFER lb_table FOR gsm_profile_numeric_value.      /* Used for recursive relationships */
DEFINE BUFFER lb1_table FOR gsm_profile_numeric_value.     /* Used for lock upgrades */



/* Standard top of WRITE trigger code */
{af/sup/aftrigtopw.i}

/* properform fields if enabled for table */
IF CAN-FIND(FIRST gsc_entity_mnemonic
            WHERE gsc_entity_mnemonic.entity_mnemonic = 'gsmnv':U
              AND gsc_entity_mnemonic.auto_properform_strings = YES) THEN
  RUN af/app/afpropfrmp.p (INPUT BUFFER gsm_profile_numeric_value:HANDLE).
  



/* Generated by ICF ERwin Template */
/* gsm_profile  gsm_profile_numeric_value ON CHILD UPDATE RESTRICT */
IF NEW gsm_profile_numeric_value OR  gsm_profile_numeric_value.profile_obj <> o_gsm_profile_numeric_value.profile_obj  THEN
  DO:
    IF NOT(CAN-FIND(FIRST gsm_profile WHERE
        gsm_profile_numeric_value.profile_obj = gsm_profile.profile_obj)) THEN
              DO:
                /* Cannot update child because parent does not exist ! */
                ASSIGN lv-error = YES lv-errgrp = "AF ":U lv-errnum = 103 lv-include = "gsm_profile_numeric_value|gsm_profile":U.
                RUN error-message (lv-errgrp, lv-errnum, lv-include).
              END.
    
    
  END.








IF NOT NEW gsm_profile_numeric_value AND gsm_profile_numeric_value.{&TRIGGER_OBJ} <> o_gsm_profile_numeric_value.{&TRIGGER_OBJ} THEN
    DO:
        ASSIGN lv-error = YES lv-errgrp = "AF":U lv-errnum = 13 lv-include = "table object number":U.
        RUN error-message (lv-errgrp,lv-errnum,lv-include).
    END.

/* Customisations to WRITE trigger */
{icf/trg/gsmnvtrigw.i}



/* Update Audit Log */
IF CAN-FIND(FIRST gsc_entity_mnemonic
            WHERE gsc_entity_mnemonic.entity_mnemonic = 'gsmnv':U
              AND gsc_entity_mnemonic.auditing_enabled = YES) THEN
  RUN af/app/afauditlgp.p (INPUT "WRITE":U, INPUT "gsmnv":U, INPUT BUFFER gsm_profile_numeric_value:HANDLE, INPUT BUFFER o_gsm_profile_numeric_value:HANDLE).

/* Standard bottom of WRITE trigger code */
{af/sup/aftrigendw.i}



