/*********************************************************************
* Copyright (C) 2000 by Progress Software Corporation. All rights    *
* reserved. Prior versions of this work may contain portions         *
* contributed by participants of Possenet.                           *
*               PSC                                                  *
*                                                                    *
*********************************************************************/

TRIGGER PROCEDURE FOR WRITE OF gst_release_version OLD BUFFER o_gst_release_version.

/* Created automatically using ERwin ICF Trigger template db/af/erw/afercustrg.i
   Do not change manually. Customisations to triggers should be placed in separate
   include files pulled into the trigger. ICF auto generates write trigger custom
   include files. Delete or create customisation include files need to be created
   manually. Be sure to put the hook in ERwin directly so as not to have you changes
   overwritten.
   User defined Macro (UDP) Summary (case sensitive)
   gst_release_version           Expands to full table name, e.g. gsm_user
   %TableFLA            Expands to table unique code, e.g. gsmus
   %TableObj            If blank or not defined expands to table_obj with no prefix (framework standard)
                        If defined, uses this field as the object field
                        If set to "none" then indicates table does not have an object field
   XYZ                  Do not define so we can compare against an empty string

   See docs for explanation of replication macros 
*/   

&SCOPED-DEFINE TRIGGER_TABLE gst_release_version
&SCOPED-DEFINE TRIGGER_FLA gstrl
&SCOPED-DEFINE TRIGGER_OBJ release_version_obj


DEFINE BUFFER lb_table FOR gst_release_version.      /* Used for recursive relationships */
DEFINE BUFFER lb1_table FOR gst_release_version.     /* Used for lock upgrades */



/* Standard top of WRITE trigger code */
{af/sup/aftrigtopw.i}

/* properform fields if enabled for table */
IF CAN-FIND(FIRST gsc_entity_mnemonic
            WHERE gsc_entity_mnemonic.entity_mnemonic = 'gstrl':U
              AND gsc_entity_mnemonic.auto_properform_strings = YES) THEN
  RUN af/app/afpropfrmp.p (INPUT BUFFER gst_release_version:HANDLE).
  



/* Generated by ICF ERwin Template */
/* gst_record_version is in gst_release_version ON CHILD UPDATE RESTRICT */
IF NEW gst_release_version OR  gst_release_version.record_version_obj <> o_gst_release_version.record_version_obj  THEN
  DO:
    IF NOT(CAN-FIND(FIRST gst_record_version WHERE
        gst_release_version.record_version_obj = gst_record_version.record_version_obj)) THEN
              DO:
                /* Cannot update child because parent does not exist ! */
                ASSIGN lv-error = YES lv-errgrp = "AF ":U lv-errnum = 103 lv-include = "gst_release_version|gst_record_version":U.
                RUN error-message (lv-errgrp, lv-errnum, lv-include).
              END.
    
    
  END.

/* Generated by ICF ERwin Template */
/* gsm_release contains gst_release_version ON CHILD UPDATE RESTRICT */
IF NEW gst_release_version OR  gst_release_version.release_obj <> o_gst_release_version.release_obj  THEN
  DO:
    IF NOT(CAN-FIND(FIRST gsm_release WHERE
        gst_release_version.release_obj = gsm_release.release_obj)) THEN
              DO:
                /* Cannot update child because parent does not exist ! */
                ASSIGN lv-error = YES lv-errgrp = "AF ":U lv-errnum = 103 lv-include = "gst_release_version|gsm_release":U.
                RUN error-message (lv-errgrp, lv-errnum, lv-include).
              END.
    
    
  END.








IF NOT NEW gst_release_version AND gst_release_version.{&TRIGGER_OBJ} <> o_gst_release_version.{&TRIGGER_OBJ} THEN
    DO:
        ASSIGN lv-error = YES lv-errgrp = "AF":U lv-errnum = 13 lv-include = "table object number":U.
        RUN error-message (lv-errgrp,lv-errnum,lv-include).
    END.

/* Customisations to WRITE trigger */
{icf/trg/gstrltrigw.i}



/* Update Audit Log */
IF CAN-FIND(FIRST gsc_entity_mnemonic
            WHERE gsc_entity_mnemonic.entity_mnemonic = 'gstrl':U
              AND gsc_entity_mnemonic.auditing_enabled = YES) THEN
  RUN af/app/afauditlgp.p (INPUT "WRITE":U, INPUT "gstrl":U, INPUT BUFFER gst_release_version:HANDLE, INPUT BUFFER o_gst_release_version:HANDLE).

/* Standard bottom of WRITE trigger code */
{af/sup/aftrigendw.i}



